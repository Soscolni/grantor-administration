{
  "name": "monday-calc-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Monday app that adds formula abilities Monday lacks (mirror/linked inputs, cross-row running totals, custom expressions) and writes the result into a real, automation-usable column.",
  "scripts": {
    "start": "node app.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
