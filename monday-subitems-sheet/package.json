{
  "name": "monday-subitems-sheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bridge: Monday.com button click -> read tasks from a Google Sheet tab named after the item's סוג המענק -> create them as subitems (name=task, stage=text column). Skips if subitems already exist.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "discover": "node scripts/discover.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22"
  }
}
